#include "main.h"
#include <stdio.h>

/**
 * _strlen - outputs string length
 * @s : a pointer starting 
 *
 * return :0
 */
int _strlen(char *s)
{
int length;
while(*s =! '\0')
     { 
	length += 1;
	*s = *s + 1 ;

     }
}
